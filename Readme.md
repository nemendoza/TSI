# Viroo Studio

## Main scene

Starting from version 3.x, the Main scene comes pre-configured in the VIROO Studio template. Therefore, if any modifications are made to this scene within the VIROO project, the corresponding scene in the template must be manually updated.

This can be done by either incorporating or modifying the necessary changes or by generating a new scene from the template and replacing the existing Main scene (recommended).
